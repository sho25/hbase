begin_unit|revision:0.9.5;language:Java;cregit-version:0.0.1
begin_comment
comment|/**  * Licensed to the Apache Software Foundation (ASF) under one  * or more contributor license agreements.  See the NOTICE file  * distributed with this work for additional information  * regarding copyright ownership.  The ASF licenses this file  * to you under the Apache License, Version 2.0 (the  * "License"); you may not use this file except in compliance  * with the License.  You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_package
package|package
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hbase
operator|.
name|util
package|;
end_package

begin_import
import|import
name|java
operator|.
name|io
operator|.
name|IOException
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|conf
operator|.
name|Configuration
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|fs
operator|.
name|Path
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hbase
operator|.
name|HConstants
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hbase
operator|.
name|TableName
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hbase
operator|.
name|client
operator|.
name|RegionInfo
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hbase
operator|.
name|regionserver
operator|.
name|HRegion
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hbase
operator|.
name|regionserver
operator|.
name|HStore
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|yetus
operator|.
name|audience
operator|.
name|InterfaceAudience
import|;
end_import

begin_comment
comment|/**  * Helper class for all utilities related to archival/retrieval of HFiles  */
end_comment

begin_class
annotation|@
name|InterfaceAudience
operator|.
name|Private
specifier|public
specifier|final
class|class
name|HFileArchiveUtil
block|{
specifier|private
name|HFileArchiveUtil
parameter_list|()
block|{
comment|// non-external instantiation - util class
block|}
comment|/**    * Get the directory to archive a store directory    * @param conf {@link Configuration} to read for the archive directory name    * @param tableName table name under which the store currently lives    * @param regionName region encoded name under which the store currently lives    * @param familyName name of the family in the store    * @return {@link Path} to the directory to archive the given store or    *<tt>null</tt> if it should not be archived    */
specifier|public
specifier|static
name|Path
name|getStoreArchivePath
parameter_list|(
specifier|final
name|Configuration
name|conf
parameter_list|,
specifier|final
name|TableName
name|tableName
parameter_list|,
specifier|final
name|String
name|regionName
parameter_list|,
specifier|final
name|String
name|familyName
parameter_list|)
throws|throws
name|IOException
block|{
name|Path
name|tableArchiveDir
init|=
name|getTableArchivePath
argument_list|(
name|conf
argument_list|,
name|tableName
argument_list|)
decl_stmt|;
return|return
name|HStore
operator|.
name|getStoreHomedir
argument_list|(
name|tableArchiveDir
argument_list|,
name|regionName
argument_list|,
name|Bytes
operator|.
name|toBytes
argument_list|(
name|familyName
argument_list|)
argument_list|)
return|;
block|}
comment|/**    * Get the directory to archive a store directory    * @param conf {@link Configuration} to read for the archive directory name.    * @param region parent region information under which the store currently lives    * @param tabledir directory for the table under which the store currently lives    * @param family name of the family in the store    * @return {@link Path} to the directory to archive the given store or<tt>null</tt> if it should    *         not be archived    */
specifier|public
specifier|static
name|Path
name|getStoreArchivePath
parameter_list|(
name|Configuration
name|conf
parameter_list|,
name|RegionInfo
name|region
parameter_list|,
name|Path
name|tabledir
parameter_list|,
name|byte
index|[]
name|family
parameter_list|)
throws|throws
name|IOException
block|{
return|return
name|getStoreArchivePath
argument_list|(
name|conf
argument_list|,
name|region
argument_list|,
name|family
argument_list|)
return|;
block|}
comment|/**    * Gets the directory to archive a store directory.    * @param conf {@link Configuration} to read for the archive directory name.    * @param region parent region information under which the store currently lives    * @param family name of the family in the store    * @return {@link Path} to the directory to archive the given store or<tt>null</tt> if it should    *         not be archived    */
specifier|public
specifier|static
name|Path
name|getStoreArchivePath
parameter_list|(
name|Configuration
name|conf
parameter_list|,
name|RegionInfo
name|region
parameter_list|,
name|byte
index|[]
name|family
parameter_list|)
throws|throws
name|IOException
block|{
name|Path
name|rootDir
init|=
name|FSUtils
operator|.
name|getRootDir
argument_list|(
name|conf
argument_list|)
decl_stmt|;
name|Path
name|tableArchiveDir
init|=
name|getTableArchivePath
argument_list|(
name|rootDir
argument_list|,
name|region
operator|.
name|getTable
argument_list|()
argument_list|)
decl_stmt|;
return|return
name|HStore
operator|.
name|getStoreHomedir
argument_list|(
name|tableArchiveDir
argument_list|,
name|region
argument_list|,
name|family
argument_list|)
return|;
block|}
comment|/**    * Gets the archive directory under specified root dir. One scenario where this is useful is    * when WAL and root dir are configured under different file systems,    * i.e. root dir on S3 and WALs on HDFS.    * This is mostly useful for archiving recovered edits, when    *<b>hbase.region.archive.recovered.edits</b> is enabled.    * @param rootDir {@link Path} the root dir under which archive path should be created.    * @param region parent region information under which the store currently lives    * @param family name of the family in the store    * @return {@link Path} to the WAL FS directory to archive the given store    *         or<tt>null</tt> if it should not be archived    */
specifier|public
specifier|static
name|Path
name|getStoreArchivePathForRootDir
parameter_list|(
name|Path
name|rootDir
parameter_list|,
name|RegionInfo
name|region
parameter_list|,
name|byte
index|[]
name|family
parameter_list|)
block|{
name|Path
name|tableArchiveDir
init|=
name|getTableArchivePath
argument_list|(
name|rootDir
argument_list|,
name|region
operator|.
name|getTable
argument_list|()
argument_list|)
decl_stmt|;
return|return
name|HStore
operator|.
name|getStoreHomedir
argument_list|(
name|tableArchiveDir
argument_list|,
name|region
argument_list|,
name|family
argument_list|)
return|;
block|}
comment|/**    * Get the archive directory for a given region under the specified table    * @param tableName the table name. Cannot be null.    * @param regiondir the path to the region directory. Cannot be null.    * @return {@link Path} to the directory to archive the given region, or<tt>null</tt> if it    *         should not be archived    */
specifier|public
specifier|static
name|Path
name|getRegionArchiveDir
parameter_list|(
name|Path
name|rootDir
parameter_list|,
name|TableName
name|tableName
parameter_list|,
name|Path
name|regiondir
parameter_list|)
block|{
comment|// get the archive directory for a table
name|Path
name|archiveDir
init|=
name|getTableArchivePath
argument_list|(
name|rootDir
argument_list|,
name|tableName
argument_list|)
decl_stmt|;
comment|// then add on the region path under the archive
name|String
name|encodedRegionName
init|=
name|regiondir
operator|.
name|getName
argument_list|()
decl_stmt|;
return|return
name|HRegion
operator|.
name|getRegionDir
argument_list|(
name|archiveDir
argument_list|,
name|encodedRegionName
argument_list|)
return|;
block|}
comment|/**    * Get the archive directory for a given region under the specified table    * @param rootDir {@link Path} to the root directory where hbase files are stored (for building    *          the archive path)    * @param tableName name of the table to archive. Cannot be null.    * @return {@link Path} to the directory to archive the given region, or<tt>null</tt> if it    *         should not be archived    */
specifier|public
specifier|static
name|Path
name|getRegionArchiveDir
parameter_list|(
name|Path
name|rootDir
parameter_list|,
name|TableName
name|tableName
parameter_list|,
name|String
name|encodedRegionName
parameter_list|)
block|{
comment|// get the archive directory for a table
name|Path
name|archiveDir
init|=
name|getTableArchivePath
argument_list|(
name|rootDir
argument_list|,
name|tableName
argument_list|)
decl_stmt|;
return|return
name|HRegion
operator|.
name|getRegionDir
argument_list|(
name|archiveDir
argument_list|,
name|encodedRegionName
argument_list|)
return|;
block|}
comment|/**    * Get the path to the table archive directory based on the configured archive directory.    *<p>    * Get the path to the table's archive directory.    *<p>    * Generally of the form: /hbase/.archive/[tablename]    * @param rootdir {@link Path} to the root directory where hbase files are stored (for building    *          the archive path)    * @param tableName Name of the table to be archived. Cannot be null.    * @return {@link Path} to the archive directory for the table    */
specifier|public
specifier|static
name|Path
name|getTableArchivePath
parameter_list|(
specifier|final
name|Path
name|rootdir
parameter_list|,
specifier|final
name|TableName
name|tableName
parameter_list|)
block|{
return|return
name|FSUtils
operator|.
name|getTableDir
argument_list|(
name|getArchivePath
argument_list|(
name|rootdir
argument_list|)
argument_list|,
name|tableName
argument_list|)
return|;
block|}
comment|/**    * Get the path to the table archive directory based on the configured archive directory.    *<p>    * Assumed that the table should already be archived.    * @param conf {@link Configuration} to read the archive directory property. Can be null    * @param tableName Name of the table to be archived. Cannot be null.    * @return {@link Path} to the archive directory for the table    */
specifier|public
specifier|static
name|Path
name|getTableArchivePath
parameter_list|(
specifier|final
name|Configuration
name|conf
parameter_list|,
specifier|final
name|TableName
name|tableName
parameter_list|)
throws|throws
name|IOException
block|{
return|return
name|FSUtils
operator|.
name|getTableDir
argument_list|(
name|getArchivePath
argument_list|(
name|conf
argument_list|)
argument_list|,
name|tableName
argument_list|)
return|;
block|}
comment|/**    * Get the full path to the archive directory on the configured    * {@link org.apache.hadoop.hbase.master.MasterFileSystem}    * @param conf to look for archive directory name and root directory. Cannot be null. Notes for    *          testing: requires a FileSystem root directory to be specified.    * @return the full {@link Path} to the archive directory, as defined by the configuration    * @throws IOException if an unexpected error occurs    */
specifier|public
specifier|static
name|Path
name|getArchivePath
parameter_list|(
name|Configuration
name|conf
parameter_list|)
throws|throws
name|IOException
block|{
return|return
name|getArchivePath
argument_list|(
name|FSUtils
operator|.
name|getRootDir
argument_list|(
name|conf
argument_list|)
argument_list|)
return|;
block|}
comment|/**    * Get the full path to the archive directory on the configured    * {@link org.apache.hadoop.hbase.master.MasterFileSystem}    * @param rootdir {@link Path} to the root directory where hbase files are stored (for building    *          the archive path)    * @return the full {@link Path} to the archive directory, as defined by the configuration    */
specifier|private
specifier|static
name|Path
name|getArchivePath
parameter_list|(
specifier|final
name|Path
name|rootdir
parameter_list|)
block|{
return|return
operator|new
name|Path
argument_list|(
name|rootdir
argument_list|,
name|HConstants
operator|.
name|HFILE_ARCHIVE_DIRECTORY
argument_list|)
return|;
block|}
comment|/*    * @return table name given archive file path    */
specifier|public
specifier|static
name|TableName
name|getTableName
parameter_list|(
name|Path
name|archivePath
parameter_list|)
block|{
name|Path
name|p
init|=
name|archivePath
decl_stmt|;
name|String
name|tbl
init|=
literal|null
decl_stmt|;
comment|// namespace is the 4th parent of file
for|for
control|(
name|int
name|i
init|=
literal|0
init|;
name|i
operator|<
literal|5
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|p
operator|==
literal|null
condition|)
return|return
literal|null
return|;
if|if
condition|(
name|i
operator|==
literal|3
condition|)
name|tbl
operator|=
name|p
operator|.
name|getName
argument_list|()
expr_stmt|;
name|p
operator|=
name|p
operator|.
name|getParent
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
name|p
operator|==
literal|null
condition|)
return|return
literal|null
return|;
return|return
name|TableName
operator|.
name|valueOf
argument_list|(
name|p
operator|.
name|getName
argument_list|()
argument_list|,
name|tbl
argument_list|)
return|;
block|}
block|}
end_class

end_unit

