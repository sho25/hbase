begin_unit|revision:0.9.5;language:Java;cregit-version:0.0.1
begin_comment
comment|/**  * Licensed to the Apache Software Foundation (ASF) under one  * or more contributor license agreements.  See the NOTICE file  * distributed with this work for additional information  * regarding copyright ownership.  The ASF licenses this file  * to you under the Apache License, Version 2.0 (the  * "License"); you may not use this file except in compliance  * with the License.  You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_package
package|package
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hbase
operator|.
name|util
package|;
end_package

begin_import
import|import
name|java
operator|.
name|io
operator|.
name|IOException
import|;
end_import

begin_import
import|import
name|java
operator|.
name|io
operator|.
name|InterruptedIOException
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|concurrent
operator|.
name|ConcurrentHashMap
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|concurrent
operator|.
name|ConcurrentMap
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|yetus
operator|.
name|audience
operator|.
name|InterfaceAudience
import|;
end_import

begin_import
import|import
name|org
operator|.
name|slf4j
operator|.
name|Logger
import|;
end_import

begin_import
import|import
name|org
operator|.
name|slf4j
operator|.
name|LoggerFactory
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hbase
operator|.
name|thirdparty
operator|.
name|com
operator|.
name|google
operator|.
name|common
operator|.
name|annotations
operator|.
name|VisibleForTesting
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hbase
operator|.
name|thirdparty
operator|.
name|com
operator|.
name|google
operator|.
name|common
operator|.
name|base
operator|.
name|Preconditions
import|;
end_import

begin_comment
comment|/**  * Allows multiple concurrent clients to lock on a numeric id with a minimal  * memory overhead. The intended usage is as follows:  *  *<pre>  * IdLock.Entry lockEntry = idLock.getLockEntry(id);  * try {  *   // User code.  * } finally {  *   idLock.releaseLockEntry(lockEntry);  * }</pre>  */
end_comment

begin_class
annotation|@
name|InterfaceAudience
operator|.
name|Private
specifier|public
class|class
name|IdLock
block|{
specifier|private
specifier|static
specifier|final
name|Logger
name|LOG
init|=
name|LoggerFactory
operator|.
name|getLogger
argument_list|(
name|IdLock
operator|.
name|class
argument_list|)
decl_stmt|;
comment|/** An entry returned to the client as a lock object */
specifier|public
specifier|static
specifier|final
class|class
name|Entry
block|{
specifier|private
specifier|final
name|long
name|id
decl_stmt|;
specifier|private
name|int
name|numWaiters
decl_stmt|;
specifier|private
name|boolean
name|locked
init|=
literal|true
decl_stmt|;
specifier|private
name|Thread
name|holder
decl_stmt|;
specifier|private
name|Entry
parameter_list|(
name|long
name|id
parameter_list|,
name|Thread
name|holder
parameter_list|)
block|{
name|this
operator|.
name|id
operator|=
name|id
expr_stmt|;
name|this
operator|.
name|holder
operator|=
name|holder
expr_stmt|;
block|}
annotation|@
name|Override
specifier|public
name|String
name|toString
parameter_list|()
block|{
return|return
literal|"id="
operator|+
name|id
operator|+
literal|", numWaiter="
operator|+
name|numWaiters
operator|+
literal|", isLocked="
operator|+
name|locked
operator|+
literal|", holder="
operator|+
name|holder
return|;
block|}
block|}
specifier|private
name|ConcurrentMap
argument_list|<
name|Long
argument_list|,
name|Entry
argument_list|>
name|map
init|=
operator|new
name|ConcurrentHashMap
argument_list|<>
argument_list|()
decl_stmt|;
comment|/**    * Blocks until the lock corresponding to the given id is acquired.    *    * @param id an arbitrary number to lock on    * @return an "entry" to pass to {@link #releaseLockEntry(Entry)} to release    *         the lock    * @throws IOException if interrupted    */
specifier|public
name|Entry
name|getLockEntry
parameter_list|(
name|long
name|id
parameter_list|)
throws|throws
name|IOException
block|{
name|Thread
name|currentThread
init|=
name|Thread
operator|.
name|currentThread
argument_list|()
decl_stmt|;
name|Entry
name|entry
init|=
operator|new
name|Entry
argument_list|(
name|id
argument_list|,
name|currentThread
argument_list|)
decl_stmt|;
name|Entry
name|existing
decl_stmt|;
while|while
condition|(
operator|(
name|existing
operator|=
name|map
operator|.
name|putIfAbsent
argument_list|(
name|entry
operator|.
name|id
argument_list|,
name|entry
argument_list|)
operator|)
operator|!=
literal|null
condition|)
block|{
synchronized|synchronized
init|(
name|existing
init|)
block|{
if|if
condition|(
name|existing
operator|.
name|locked
condition|)
block|{
operator|++
name|existing
operator|.
name|numWaiters
expr_stmt|;
comment|// Add ourselves to waiters.
while|while
condition|(
name|existing
operator|.
name|locked
condition|)
block|{
try|try
block|{
name|existing
operator|.
name|wait
argument_list|()
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|InterruptedException
name|e
parameter_list|)
block|{
operator|--
name|existing
operator|.
name|numWaiters
expr_stmt|;
comment|// Remove ourselves from waiters.
comment|// HBASE-21292
comment|// There is a rare case that interrupting and the lock owner thread call
comment|// releaseLockEntry at the same time. Since the owner thread found there
comment|// still one waiting, it won't remove the entry from the map. If the interrupted
comment|// thread is the last one waiting on the lock, and since an exception is thrown,
comment|// the 'existing' entry will stay in the map forever. Later threads which try to
comment|// get this lock will stuck in a infinite loop because
comment|// existing = map.putIfAbsent(entry.id, entry)) != null and existing.locked=false.
if|if
condition|(
operator|!
name|existing
operator|.
name|locked
operator|&&
name|existing
operator|.
name|numWaiters
operator|==
literal|0
condition|)
block|{
name|map
operator|.
name|remove
argument_list|(
name|existing
operator|.
name|id
argument_list|)
expr_stmt|;
block|}
throw|throw
operator|new
name|InterruptedIOException
argument_list|(
literal|"Interrupted waiting to acquire sparse lock"
argument_list|)
throw|;
block|}
block|}
operator|--
name|existing
operator|.
name|numWaiters
expr_stmt|;
comment|// Remove ourselves from waiters.
name|existing
operator|.
name|locked
operator|=
literal|true
expr_stmt|;
name|existing
operator|.
name|holder
operator|=
name|currentThread
expr_stmt|;
return|return
name|existing
return|;
block|}
comment|// If the entry is not locked, it might already be deleted from the
comment|// map, so we cannot return it. We need to get our entry into the map
comment|// or get someone else's locked entry.
block|}
block|}
return|return
name|entry
return|;
block|}
comment|/**    * Blocks until the lock corresponding to the given id is acquired.    *    * @param id an arbitrary number to lock on    * @param time time to wait in ms    * @return an "entry" to pass to {@link #releaseLockEntry(Entry)} to release    *         the lock    * @throws IOException if interrupted    */
specifier|public
name|Entry
name|tryLockEntry
parameter_list|(
name|long
name|id
parameter_list|,
name|long
name|time
parameter_list|)
throws|throws
name|IOException
block|{
name|Preconditions
operator|.
name|checkArgument
argument_list|(
name|time
operator|>=
literal|0
argument_list|)
expr_stmt|;
name|Thread
name|currentThread
init|=
name|Thread
operator|.
name|currentThread
argument_list|()
decl_stmt|;
name|Entry
name|entry
init|=
operator|new
name|Entry
argument_list|(
name|id
argument_list|,
name|currentThread
argument_list|)
decl_stmt|;
name|Entry
name|existing
decl_stmt|;
name|long
name|waitUtilTS
init|=
name|System
operator|.
name|currentTimeMillis
argument_list|()
operator|+
name|time
decl_stmt|;
name|long
name|remaining
init|=
name|time
decl_stmt|;
while|while
condition|(
operator|(
name|existing
operator|=
name|map
operator|.
name|putIfAbsent
argument_list|(
name|entry
operator|.
name|id
argument_list|,
name|entry
argument_list|)
operator|)
operator|!=
literal|null
condition|)
block|{
synchronized|synchronized
init|(
name|existing
init|)
block|{
if|if
condition|(
name|existing
operator|.
name|locked
condition|)
block|{
operator|++
name|existing
operator|.
name|numWaiters
expr_stmt|;
comment|// Add ourselves to waiters.
try|try
block|{
while|while
condition|(
name|existing
operator|.
name|locked
condition|)
block|{
name|existing
operator|.
name|wait
argument_list|(
name|remaining
argument_list|)
expr_stmt|;
if|if
condition|(
name|existing
operator|.
name|locked
condition|)
block|{
name|long
name|currentTS
init|=
name|System
operator|.
name|currentTimeMillis
argument_list|()
decl_stmt|;
if|if
condition|(
name|currentTS
operator|>=
name|waitUtilTS
condition|)
block|{
comment|// time is up
return|return
literal|null
return|;
block|}
else|else
block|{
comment|// our wait is waken, but the lock is still taken, this can happen
comment|// due to JDK Object's wait/notify mechanism.
comment|// Calculate the new remaining time to wait
name|remaining
operator|=
name|waitUtilTS
operator|-
name|currentTS
expr_stmt|;
block|}
block|}
block|}
block|}
catch|catch
parameter_list|(
name|InterruptedException
name|e
parameter_list|)
block|{
comment|// HBASE-21292
comment|// Please refer to the comments in getLockEntry()
comment|// the difference here is that we decrease numWaiters in finally block
if|if
condition|(
operator|!
name|existing
operator|.
name|locked
operator|&&
name|existing
operator|.
name|numWaiters
operator|==
literal|1
condition|)
block|{
name|map
operator|.
name|remove
argument_list|(
name|existing
operator|.
name|id
argument_list|)
expr_stmt|;
block|}
throw|throw
operator|new
name|InterruptedIOException
argument_list|(
literal|"Interrupted waiting to acquire sparse lock"
argument_list|)
throw|;
block|}
finally|finally
block|{
operator|--
name|existing
operator|.
name|numWaiters
expr_stmt|;
comment|// Remove ourselves from waiters.
block|}
name|existing
operator|.
name|locked
operator|=
literal|true
expr_stmt|;
name|existing
operator|.
name|holder
operator|=
name|currentThread
expr_stmt|;
return|return
name|existing
return|;
block|}
comment|// If the entry is not locked, it might already be deleted from the
comment|// map, so we cannot return it. We need to get our entry into the map
comment|// or get someone else's locked entry.
block|}
block|}
return|return
name|entry
return|;
block|}
comment|/**    * Must be called in a finally block to decrease the internal counter and remove the monitor    * object for the given id if the caller is the last client.    * @param entry the return value of {@link #getLockEntry(long)}    */
specifier|public
name|void
name|releaseLockEntry
parameter_list|(
name|Entry
name|entry
parameter_list|)
block|{
name|Thread
name|currentThread
init|=
name|Thread
operator|.
name|currentThread
argument_list|()
decl_stmt|;
synchronized|synchronized
init|(
name|entry
init|)
block|{
if|if
condition|(
name|entry
operator|.
name|holder
operator|!=
name|currentThread
condition|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"{} is trying to release lock entry {}, but it is not the holder."
argument_list|,
name|currentThread
argument_list|,
name|entry
argument_list|)
expr_stmt|;
block|}
name|entry
operator|.
name|locked
operator|=
literal|false
expr_stmt|;
if|if
condition|(
name|entry
operator|.
name|numWaiters
operator|>
literal|0
condition|)
block|{
name|entry
operator|.
name|notify
argument_list|()
expr_stmt|;
block|}
else|else
block|{
name|map
operator|.
name|remove
argument_list|(
name|entry
operator|.
name|id
argument_list|)
expr_stmt|;
block|}
block|}
block|}
comment|/**    * Test whether the given id is already locked by the current thread.    */
specifier|public
name|boolean
name|isHeldByCurrentThread
parameter_list|(
name|long
name|id
parameter_list|)
block|{
name|Thread
name|currentThread
init|=
name|Thread
operator|.
name|currentThread
argument_list|()
decl_stmt|;
name|Entry
name|entry
init|=
name|map
operator|.
name|get
argument_list|(
name|id
argument_list|)
decl_stmt|;
if|if
condition|(
name|entry
operator|==
literal|null
condition|)
block|{
return|return
literal|false
return|;
block|}
synchronized|synchronized
init|(
name|entry
init|)
block|{
return|return
name|currentThread
operator|.
name|equals
argument_list|(
name|entry
operator|.
name|holder
argument_list|)
return|;
block|}
block|}
annotation|@
name|VisibleForTesting
name|void
name|assertMapEmpty
parameter_list|()
block|{
assert|assert
name|map
operator|.
name|isEmpty
argument_list|()
assert|;
block|}
annotation|@
name|VisibleForTesting
specifier|public
name|void
name|waitForWaiters
parameter_list|(
name|long
name|id
parameter_list|,
name|int
name|numWaiters
parameter_list|)
throws|throws
name|InterruptedException
block|{
for|for
control|(
name|Entry
name|entry
init|;
condition|;
control|)
block|{
name|entry
operator|=
name|map
operator|.
name|get
argument_list|(
name|id
argument_list|)
expr_stmt|;
if|if
condition|(
name|entry
operator|!=
literal|null
condition|)
block|{
synchronized|synchronized
init|(
name|entry
init|)
block|{
if|if
condition|(
name|entry
operator|.
name|numWaiters
operator|>=
name|numWaiters
condition|)
block|{
return|return;
block|}
block|}
block|}
name|Thread
operator|.
name|sleep
argument_list|(
literal|100
argument_list|)
expr_stmt|;
block|}
block|}
block|}
end_class

end_unit

